# The Query type represents all of the entry points into the API.
type Query {
  # Retrieve WIT for a given space
  filter(spaceId: String!): [WorkItem!]
  # Retrieve all WIT
  workItems: [WorkItem]
}
